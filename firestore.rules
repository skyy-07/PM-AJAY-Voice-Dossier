rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for PM-AJAY prototype demo and authenticated or anonymous clients
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
